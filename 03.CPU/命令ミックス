CPUの基本的な命令手順というのが、
①命令の取り出し=>②命令の解読=>③対象データ読み出し=>④命令実行である。

でも、対象データ呼び出しが必要ない命令だと③は必要ない。

つまり、命令というのは、
その命令によって必要なクロックサイクル数が異なっていたりする。
(3クロックで終わるものもあれば5クロック必要なものもある)

MIPSはどの命令で計算するのか？↓

そこで用いられるのが命令ミックスです。
命令ミックス = よく使われる命令を一つにしたもの。