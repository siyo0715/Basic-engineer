画像データは点の情報を集めたもの

コンピュータの扱う、代表的な画像データのあらわし方はビットマップ方式です。
これは、画像を細かいビットで表現します。

画像をどれだけ細かく刻むかが解像度。
これが高いほど画像は滑らかになります。

例えば640 * 480ドットの画像データだった場合。
その画像を構成するドットの数は307,200個です。

ドットの集まりを絵にするには、
「そのドットは何色か？」という情報が必要。
そんなわけで、
ドット一つ一つに色情報がぶら下がります。